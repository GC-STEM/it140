{
  "id": "win-verify-manifest-failure",
  "arguments": ["-DeploymentProfile", "windows_bare_metal", "-SkipNetwork"],
  "fixture_overrides": {
    "manifest": "malformed"
  },
  "expected": {
    "exit_code": 5,
    "checks": {
      "verify.manifest": "FAIL"
    },
    "failed": 1
  }
}
