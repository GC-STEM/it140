{
  "id": "win-install-privilege-failure",
  "arguments": ["-DeploymentProfile", "windows_bare_metal"],
  "state_overrides": {"is_administrator": false},
  "expected": {
    "exit_code": 3,
    "result": "FAIL",
    "managed_changes": "No",
    "failures": 1,
    "output_contains": ["Setup requires an elevated Windows PowerShell terminal"]
  }
}
